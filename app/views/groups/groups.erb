<h1> Welcome to Your Groups, <%= current_user.username.capitalize %> !</h1>





<% if !@groups.empty? %>

<% @groups.each do |t| %>
  <p><%= t.name %> <%= t.user_id%> <%= t.id%></p>
  <% end %>



<% else %>

POO

<% end %>


<h1> Your Groups </h1>

<% @groups.each do |t| %>
  <% if t.user_id == current_user.id %>
 <div><a href="/groups/<%= t.id %>/edit"><span class="#"></span> Edit</div>
    <div><%= t.name %> <%= t.user_id%> <%= t.id%></div>
  <% end %>
<% end %>
